export interface User {
  id: string;
  name: string;
  avatar: string;
}
// type User = {
//   id: string;
//   name: string;
//   avatar: string;
// };            //Object created

// same as object, it is an interface both working is same.
